import { describe, it, expect } from 'vitest'

/**
 * Sample unit test to verify Vitest configuration works correctly.
 * This test verifies:
 * - Vitest is properly configured
 * - Test discovery works with tests directory pattern
 * - Basic assertions work
 */
describe('Sample Unit Test', () => {
  it('should pass basic assertions', () => {
    expect(1 + 1).toBe(2)
  })
})
